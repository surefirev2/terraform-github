# Repository rename: terraform-cloudflare -> terraform-cloudflare-surefire
moved {
  from = github_repository.repos["terraform-cloudflare"]
  to   = github_repository.repos["terraform-cloudflare-surefire"]
}
