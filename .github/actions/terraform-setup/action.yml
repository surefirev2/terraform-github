# Synced from template — do not edit. Changes will be overwritten on the next sync.
# Shared Terraform CI setup: build image, init.
# Requires: checkout, docker/setup-buildx, and .env already created (e.g. by terraform-load-env.sh after 1Password).
name: Terraform setup
description: Build Terraform image and run terraform init
runs:
  using: composite
  steps:
    - name: Build Docker image
      run: make build-image
      shell: bash

    - name: Initialize Terraform
      run: make init
      shell: bash
